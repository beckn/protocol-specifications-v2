# AUTO-GENERATED — DO NOT EDIT DIRECTLY
# Sources: api/v2.0.0/components/io/core.yaml  |  api/v2.0.0/components/schema/core.yaml  |  github.com/beckn/core_schema
# Regenerate: cd protocol-specifications-v2 && python3 scripts/rebuild_beckn_yaml.py
#
openapi: 3.1.1
info:
  title: Beckn Protocol API Specification Version 2
  description: |
    The API specification of Beckn Protocol — a fully resolved, concrete OpenAPI
    specification with one named path per known Beckn endpoint.

    **Interaction model:** All Beckn interactions are asynchronous. The caller sends
    a request and receives a synchronous `Ack` (HTTP 200). The actual response
    arrives as a separate callback on the caller's registered callback endpoint
    using the corresponding `on_<action>` endpoint.

    **Authentication:** All requests MUST carry a Beckn Signature in the
    `Authorization` header. See the `Signature` schema for the expected format.

    ---

    ⚠️ **THIS FILE IS AUTO-GENERATED. Do not edit directly.**

    | Source | Path |
    |--------|------|
    | Abstract IO spec | `api/v2.0.0/components/io/core.yaml` |
    | Transport schemas | `api/v2.0.0/components/schema/core.yaml` |
    | Data model schemas | `https://github.com/beckn/core_schema` |
    | Generator | `scripts/generate_endpoints.py` |

    Regenerate: `cd protocol-specifications-v2 && python3 scripts/generate_endpoints.py`
  version: 2.0.0
  contact:
    name: Beckn Protocol
    url: https://beckn.io
  license:
    name: CC-BY-NC-SA 4.0 International
    url: https://creativecommons.org/licenses/by-nc-sa/4.0/deed.en
paths:
  /beckn/discover:
    post:
      summary: Discover — BAP sends a discovery request
      description: |-
        BAP sends a discovery intent to one or more BPPs to find catalogs matching the consumer's requirements. The intent may carry a free-text query, structured filters, spatial constraints, or media inputs.

        **Direction:** BAP → BPP
      operationId: beckn_discover
      tags:
        - Discover
      parameters:
        - $ref: '#/components/parameters/AuthorizationHeader'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: https://schema.beckn.io/DiscoverAction/v2.0
      responses:
        '200':
          $ref: '#/components/responses/Ack'
        '400':
          $ref: '#/components/responses/NackBadRequest'
        '401':
          $ref: '#/components/responses/NackUnauthorized'
        '409':
          $ref: '#/components/responses/AckNoCallback'
        '500':
          $ref: '#/components/responses/ServerError'
  /beckn/on_discover:
    post:
      summary: On-Discover — BPP returns matching catalogs
      description: |-
        BPP responds to a discover request with catalogs that match the expressed intent. Each catalog contains the provider's items, offers, fulfillment options, and pricing.

        **Direction:** BPP → BAP (async callback)
      operationId: beckn_on_discover
      tags:
        - Discover
      parameters:
        - $ref: '#/components/parameters/AuthorizationHeader'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: https://schema.beckn.io/OnDiscoverAction/v2.0
      responses:
        '200':
          $ref: '#/components/responses/Ack'
        '400':
          $ref: '#/components/responses/NackBadRequest'
        '401':
          $ref: '#/components/responses/NackUnauthorized'
        '409':
          $ref: '#/components/responses/AckNoCallback'
        '500':
          $ref: '#/components/responses/ServerError'
  /beckn/select:
    post:
      summary: Select — BAP selects items and requests a quote
      description: |-
        BAP sends the consumer's selected items and asks the BPP to generate a draft contract with pricing, taxes, and available fulfillment options.

        **Direction:** BAP → BPP
      operationId: beckn_select
      tags:
        - Select
      parameters:
        - $ref: '#/components/parameters/AuthorizationHeader'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: https://schema.beckn.io/SelectAction/v2.0
      responses:
        '200':
          $ref: '#/components/responses/Ack'
        '400':
          $ref: '#/components/responses/NackBadRequest'
        '401':
          $ref: '#/components/responses/NackUnauthorized'
        '409':
          $ref: '#/components/responses/AckNoCallback'
        '500':
          $ref: '#/components/responses/ServerError'
  /beckn/on_select:
    post:
      summary: On-Select — BPP returns a draft contract with quote
      description: |-
        BPP responds with a draft contract reflecting the selected items, computed pricing, applicable taxes, and fulfillment options.

        **Direction:** BPP → BAP (async callback)
      operationId: beckn_on_select
      tags:
        - Select
      parameters:
        - $ref: '#/components/parameters/AuthorizationHeader'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: https://schema.beckn.io/OnSelectAction/v2.0
      responses:
        '200':
          $ref: '#/components/responses/Ack'
        '400':
          $ref: '#/components/responses/NackBadRequest'
        '401':
          $ref: '#/components/responses/NackUnauthorized'
        '409':
          $ref: '#/components/responses/AckNoCallback'
        '500':
          $ref: '#/components/responses/ServerError'
  /beckn/init:
    post:
      summary: Init — BAP submits billing and fulfillment details
      description: |-
        BAP submits the consumer's billing address, chosen fulfillment option, and preferred payment method to initiate an order.

        **Direction:** BAP → BPP
      operationId: beckn_init
      tags:
        - Init
      parameters:
        - $ref: '#/components/parameters/AuthorizationHeader'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: https://schema.beckn.io/InitAction/v2.0
      responses:
        '200':
          $ref: '#/components/responses/Ack'
        '400':
          $ref: '#/components/responses/NackBadRequest'
        '401':
          $ref: '#/components/responses/NackUnauthorized'
        '409':
          $ref: '#/components/responses/AckNoCallback'
        '500':
          $ref: '#/components/responses/ServerError'
  /beckn/on_init:
    post:
      summary: On-Init — BPP returns final terms and payment instructions
      description: |-
        BPP responds with the final order terms, complete price breakdown, and payment/settlement instructions. The consumer reviews these terms before confirming.

        **Direction:** BPP → BAP (async callback)
      operationId: beckn_on_init
      tags:
        - Init
      parameters:
        - $ref: '#/components/parameters/AuthorizationHeader'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: https://schema.beckn.io/OnInitAction/v2.0
      responses:
        '200':
          $ref: '#/components/responses/Ack'
        '400':
          $ref: '#/components/responses/NackBadRequest'
        '401':
          $ref: '#/components/responses/NackUnauthorized'
        '409':
          $ref: '#/components/responses/AckNoCallback'
        '500':
          $ref: '#/components/responses/ServerError'
  /beckn/confirm:
    post:
      summary: Confirm — BAP confirms the order
      description: |-
        BAP confirms the order after the consumer has reviewed and accepted the final terms returned in on_init. This creates a binding order.

        **Direction:** BAP → BPP
      operationId: beckn_confirm
      tags:
        - Confirm
      parameters:
        - $ref: '#/components/parameters/AuthorizationHeader'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: https://schema.beckn.io/ConfirmAction/v2.0
      responses:
        '200':
          $ref: '#/components/responses/Ack'
        '400':
          $ref: '#/components/responses/NackBadRequest'
        '401':
          $ref: '#/components/responses/NackUnauthorized'
        '409':
          $ref: '#/components/responses/AckNoCallback'
        '500':
          $ref: '#/components/responses/ServerError'
  /beckn/on_confirm:
    post:
      summary: On-Confirm — BPP acknowledges order confirmation
      description: |-
        BPP acknowledges the confirmed order and returns the active contract with the assigned order identifier and fulfillment state.

        **Direction:** BPP → BAP (async callback)
      operationId: beckn_on_confirm
      tags:
        - Confirm
      parameters:
        - $ref: '#/components/parameters/AuthorizationHeader'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: https://schema.beckn.io/OnConfirmAction/v2.0
      responses:
        '200':
          $ref: '#/components/responses/Ack'
        '400':
          $ref: '#/components/responses/NackBadRequest'
        '401':
          $ref: '#/components/responses/NackUnauthorized'
        '409':
          $ref: '#/components/responses/AckNoCallback'
        '500':
          $ref: '#/components/responses/ServerError'
  /beckn/status:
    post:
      summary: Status — BAP queries current order status
      description: |-
        BAP requests the current status of a previously confirmed order by providing the order identifier.

        **Direction:** BAP → BPP
      operationId: beckn_status
      tags:
        - Status
      parameters:
        - $ref: '#/components/parameters/AuthorizationHeader'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: https://schema.beckn.io/StatusAction/v2.0
      responses:
        '200':
          $ref: '#/components/responses/Ack'
        '400':
          $ref: '#/components/responses/NackBadRequest'
        '401':
          $ref: '#/components/responses/NackUnauthorized'
        '409':
          $ref: '#/components/responses/AckNoCallback'
        '500':
          $ref: '#/components/responses/ServerError'
  /beckn/on_status:
    post:
      summary: On-Status — BPP returns current order status
      description: |-
        BPP returns the current state of the order, including the latest fulfillment stage, tracking state, and any active alerts.

        **Direction:** BPP → BAP (async callback)
      operationId: beckn_on_status
      tags:
        - Status
      parameters:
        - $ref: '#/components/parameters/AuthorizationHeader'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: https://schema.beckn.io/OnStatusAction/v2.0
      responses:
        '200':
          $ref: '#/components/responses/Ack'
        '400':
          $ref: '#/components/responses/NackBadRequest'
        '401':
          $ref: '#/components/responses/NackUnauthorized'
        '409':
          $ref: '#/components/responses/AckNoCallback'
        '500':
          $ref: '#/components/responses/ServerError'
  /beckn/track:
    post:
      summary: Track — BAP requests a real-time tracking link
      description: |-
        BAP requests a real-time tracking URL or WebSocket handle for an active fulfillment leg.

        **Direction:** BAP → BPP
      operationId: beckn_track
      tags:
        - Track
      parameters:
        - $ref: '#/components/parameters/AuthorizationHeader'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: https://schema.beckn.io/TrackAction/v2.0
      responses:
        '200':
          $ref: '#/components/responses/Ack'
        '400':
          $ref: '#/components/responses/NackBadRequest'
        '401':
          $ref: '#/components/responses/NackUnauthorized'
        '409':
          $ref: '#/components/responses/AckNoCallback'
        '500':
          $ref: '#/components/responses/ServerError'
  /beckn/on_track:
    post:
      summary: On-Track — BPP returns tracking information
      description: |-
        BPP returns a tracking URL, WebSocket handle, or embedded real-time tracking feed for the active fulfillment.

        **Direction:** BPP → BAP (async callback)
      operationId: beckn_on_track
      tags:
        - Track
      parameters:
        - $ref: '#/components/parameters/AuthorizationHeader'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: https://schema.beckn.io/OnTrackAction/v2.0
      responses:
        '200':
          $ref: '#/components/responses/Ack'
        '400':
          $ref: '#/components/responses/NackBadRequest'
        '401':
          $ref: '#/components/responses/NackUnauthorized'
        '409':
          $ref: '#/components/responses/AckNoCallback'
        '500':
          $ref: '#/components/responses/ServerError'
  /beckn/update:
    post:
      summary: Update — BAP updates an active order
      description: |-
        BAP requests a modification to an active order, such as changing the fulfillment address, adjusting quantities, or adding items.

        **Direction:** BAP → BPP
      operationId: beckn_update
      tags:
        - Update
      parameters:
        - $ref: '#/components/parameters/AuthorizationHeader'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: https://schema.beckn.io/UpdateAction/v2.0
      responses:
        '200':
          $ref: '#/components/responses/Ack'
        '400':
          $ref: '#/components/responses/NackBadRequest'
        '401':
          $ref: '#/components/responses/NackUnauthorized'
        '409':
          $ref: '#/components/responses/AckNoCallback'
        '500':
          $ref: '#/components/responses/ServerError'
  /beckn/on_update:
    post:
      summary: On-Update — BPP returns the updated contract
      description: |-
        BPP returns the updated contract reflecting the applied changes, including any pricing adjustments.

        **Direction:** BPP → BAP (async callback)
      operationId: beckn_on_update
      tags:
        - Update
      parameters:
        - $ref: '#/components/parameters/AuthorizationHeader'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: https://schema.beckn.io/OnUpdateAction/v2.0
      responses:
        '200':
          $ref: '#/components/responses/Ack'
        '400':
          $ref: '#/components/responses/NackBadRequest'
        '401':
          $ref: '#/components/responses/NackUnauthorized'
        '409':
          $ref: '#/components/responses/AckNoCallback'
        '500':
          $ref: '#/components/responses/ServerError'
  /beckn/cancel:
    post:
      summary: Cancel — BAP cancels an active order
      description: |-
        BAP cancels an active order, providing a cancellation reason code as defined in the applicable cancellation policy.

        **Direction:** BAP → BPP
      operationId: beckn_cancel
      tags:
        - Cancel
      parameters:
        - $ref: '#/components/parameters/AuthorizationHeader'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: https://schema.beckn.io/CancelAction/v2.0
      responses:
        '200':
          $ref: '#/components/responses/Ack'
        '400':
          $ref: '#/components/responses/NackBadRequest'
        '401':
          $ref: '#/components/responses/NackUnauthorized'
        '409':
          $ref: '#/components/responses/AckNoCallback'
        '500':
          $ref: '#/components/responses/ServerError'
  /beckn/on_cancel:
    post:
      summary: On-Cancel — BPP confirms order cancellation
      description: |-
        BPP confirms the cancellation and returns the final contract state, including any applicable refund terms.

        **Direction:** BPP → BAP (async callback)
      operationId: beckn_on_cancel
      tags:
        - Cancel
      parameters:
        - $ref: '#/components/parameters/AuthorizationHeader'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: https://schema.beckn.io/OnCancelAction/v2.0
      responses:
        '200':
          $ref: '#/components/responses/Ack'
        '400':
          $ref: '#/components/responses/NackBadRequest'
        '401':
          $ref: '#/components/responses/NackUnauthorized'
        '409':
          $ref: '#/components/responses/AckNoCallback'
        '500':
          $ref: '#/components/responses/ServerError'
  /beckn/rate:
    post:
      summary: Rate — BAP submits ratings and feedback
      description: |-
        BAP submits one or more ratings for entities in the completed order (item, fulfillment, provider, or agent), optionally with feedback form submissions.

        **Direction:** BAP → BPP
      operationId: beckn_rate
      tags:
        - Rate
      parameters:
        - $ref: '#/components/parameters/AuthorizationHeader'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: https://schema.beckn.io/RateAction/v2.0
      responses:
        '200':
          $ref: '#/components/responses/Ack'
        '400':
          $ref: '#/components/responses/NackBadRequest'
        '401':
          $ref: '#/components/responses/NackUnauthorized'
        '409':
          $ref: '#/components/responses/AckNoCallback'
        '500':
          $ref: '#/components/responses/ServerError'
  /beckn/on_rate:
    post:
      summary: On-Rate — BPP returns rating forms
      description: |-
        BPP returns rating forms for the BAP to present to the consumer, enabling collection of structured feedback.

        **Direction:** BPP → BAP (async callback)
      operationId: beckn_on_rate
      tags:
        - Rate
      parameters:
        - $ref: '#/components/parameters/AuthorizationHeader'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: https://schema.beckn.io/OnRateAction/v2.0
      responses:
        '200':
          $ref: '#/components/responses/Ack'
        '400':
          $ref: '#/components/responses/NackBadRequest'
        '401':
          $ref: '#/components/responses/NackUnauthorized'
        '409':
          $ref: '#/components/responses/AckNoCallback'
        '500':
          $ref: '#/components/responses/ServerError'
  /beckn/support:
    post:
      summary: Support — BAP requests support contact information
      description: |-
        BAP requests support contact details for a specific order or entity. The consumer may also initiate a support callback request.

        **Direction:** BAP → BPP
      operationId: beckn_support
      tags:
        - Support
      parameters:
        - $ref: '#/components/parameters/AuthorizationHeader'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: https://schema.beckn.io/SupportAction/v2.0
      responses:
        '200':
          $ref: '#/components/responses/Ack'
        '400':
          $ref: '#/components/responses/NackBadRequest'
        '401':
          $ref: '#/components/responses/NackUnauthorized'
        '409':
          $ref: '#/components/responses/AckNoCallback'
        '500':
          $ref: '#/components/responses/ServerError'
  /beckn/on_support:
    post:
      summary: On-Support — BPP returns support contact details
      description: |-
        BPP returns support contact information including email address, telephone number, chat endpoint, and available support channels.

        **Direction:** BPP → BAP (async callback)
      operationId: beckn_on_support
      tags:
        - Support
      parameters:
        - $ref: '#/components/parameters/AuthorizationHeader'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: https://schema.beckn.io/OnSupportAction/v2.0
      responses:
        '200':
          $ref: '#/components/responses/Ack'
        '400':
          $ref: '#/components/responses/NackBadRequest'
        '401':
          $ref: '#/components/responses/NackUnauthorized'
        '409':
          $ref: '#/components/responses/AckNoCallback'
        '500':
          $ref: '#/components/responses/ServerError'
components:
  schemas:
    Ack:
      title: Acknowledgement
      description: Synchronous receipt acknowledgement returned for every accepted
        Beckn request.
      type: object
      properties:
        status:
          type: string
          description: ACK if the request was accepted; NACK if rejected.
          enum:
            - ACK
            - NACK
        signature:
          allOf:
            - $ref: '#/components/schemas/CounterSignature'
          description: Counter-signature proving the receiver authenticated and processed
            the inbound request.
      required:
        - status
        - signature
    AckNoCallback:
      title: Acknowledgement — No Callback
      description: |
        Request received and valid, but no asynchronous callback will follow. Typical causes:
        no matching agents found, inventory unavailable, store closed.
      allOf:
        - $ref: '#/components/schemas/Ack'
        - properties:
            error:
              allOf:
                - $ref: '#/components/schemas/Error'
              description: Optional error detail explaining why no callback will follow.
    CounterSignature:
      title: Beckn HTTP Counter-Signature
      description: |
        A signed receipt transmitted in the synchronous `Ack` response body, proving that the
        receiver authenticated, received, and processed the inbound request. It is the schema
        realisation of the Signed Receipt of Receipt concept proposed in
        [Issue #69](https://github.com/beckn/protocol-specifications-v2/issues/69).

        `CounterSignature` shares the same wire format as `Signature` but differs in three ways:

        | Aspect | `Signature` | `CounterSignature` |
        |---|---|---|
        | **Signer** | Request sender (BAP/BPP) | Response receiver (BPP/BAP) |
        | **Transmitted in** | Inbound `Authorization` header | `Ack.signature` in response body |
        | **`digest`** | BLAKE2b-512 of **inbound request** body | BLAKE2b-512 of **Ack response** body |
        | **`(request-digest)`** | Not present | MUST be present |
        | **`(message-id)`** | Not present | MUST be present |
        | **`headers`** | `"(created) (expires) digest"` | `"(created) (expires) digest (request-digest) (message-id)"` |

        **Signing string:**
        ```
        (created): {unixTimestamp}
        (expires): {unixTimestamp}
        digest: BLAKE-512={base64DigestOfAckBody}
        (request-digest): BLAKE-512={base64DigestOfInboundRequestBody}
        (message-id): {messageId}
        ```
      allOf:
        - $ref: '#/components/schemas/Signature'
      example: Signature keyId="example-bpp.com|74b43deb-236e-4498-8f5a-ca75d6c67b9d|ed25519",algorithm="ed25519",created="1641287900",expires="1641291500",headers="(created)
        (expires) digest (request-digest) (message-id)",signature="mX3sHA+9gileOkXYJXh4Jg8tK0gEEMbf9yCPnFpbldhrAY+NErqL9WD+Vav7TE5tyVXGXBle9ONZi2W7o144eQ=="
    Error:
      title: Error
      description: Base error container returned in NACK responses and async error
        callbacks.
      type: object
      properties:
        errorCode:
          type: string
          description: Machine-readable error code.
        errorMessage:
          type: string
          description: Human-readable error description.
    NackBadRequest:
      title: NACK — Bad Request
      description: Malformed or invalid request; the server could not parse or validate
        the payload.
      allOf:
        - $ref: '#/components/schemas/Ack'
        - properties:
            status:
              type: string
              const: NACK
            error:
              allOf:
                - $ref: '#/components/schemas/Error'
    NackUnauthorized:
      title: NACK — Unauthorized
      description: Invalid or missing authentication credentials; signature could
        not be verified.
      allOf:
        - $ref: '#/components/schemas/Ack'
        - properties:
            status:
              type: string
              const: NACK
            error:
              allOf:
                - $ref: '#/components/schemas/Error'
    ServerError:
      title: Server Error
      description: Internal failure on the network participant's platform; the request
        could not be processed.
      type: object
    Signature:
      title: Beckn HTTP Signature
      description: |
        A digitally signed authentication credential in the HTTP `Authorization` header.
        Follows the HTTP Signature scheme from
        [draft-cavage-http-signatures-12](https://datatracker.ietf.org/doc/html/draft-cavage-http-signatures-12)
        as profiled by [BECKN-006](https://github.com/beckn/protocol-specifications/blob/master/docs/BECKN-006-Signing-Beckn-APIs-In-HTTP-Draft-01.md).

        **Format:**
        ```
        Signature keyId="{subscriberId}|{uniqueKeyId}|{algorithm}",algorithm="{algorithm}",created="{unixTimestamp}",expires="{unixTimestamp}",headers="{signedHeaders}",signature="{base64Signature}"
        ```

        | Attribute | Description |
        |---|---|
        | `keyId` | `{subscriberId}\|{uniqueKeyId}\|{algorithm}` — FQDN, registry UUID, signing algorithm |
        | `algorithm` | MUST be `ed25519` |
        | `created` | Unix timestamp when the signature was created |
        | `expires` | Unix timestamp when the signature expires |
        | `headers` | Space-separated signed headers. MUST include `(created)`, `(expires)`, `digest` |
        | `signature` | Base64-encoded Ed25519 signature over the signing string |

        The signing string is formed by concatenating:
        `(created): {value}\n(expires): {value}\ndigest: BLAKE-512={digest}`,
        where `digest` is a BLAKE2b-512 hash of the request body, Base64-encoded.
      type: string
      pattern: ^Signature keyId="[^|"]+\|[^|"]+\|[^"]+",algorithm="[^"]+",created="\d+",expires="\d+",headers="[^"]+",signature="[A-Za-z0-9+/]+=*"$
      example: Signature keyId="example-bap.com|ae3ea24b-cfec-495e-81f8-044aaef164ac|ed25519",algorithm="ed25519",created="1641287875",expires="1641291475",headers="(created)
        (expires) digest",signature="cjbhP0PFyrlSCNszJM1F/YmHDVAWsZqJUPzojnE/7TJU3fJ/rmIlgaUHEr5E0/2PIyf0tpSnWtT6cyNNlpmoAQ=="
  parameters:
    AuthorizationHeader:
      name: Authorization
      in: header
      required: true
      description: |
        Beckn Signature authentication header (POST endpoints and GET Body Mode).
        See `#/components/schemas/Signature` for the expected format.
      schema:
        $ref: '#/components/schemas/Signature'
    AuthorizationQuery:
      name: Authorization
      in: query
      required: false
      description: |
        Beckn Signature passed as a query parameter (GET Query Mode only).
        Mutually exclusive with the `Authorization` header and the request body.
      schema:
        $ref: '#/components/schemas/Signature'
    RequestActionQuery:
      name: RequestAction
      in: query
      required: false
      description: |
        Beckn action payload passed as a URL-encoded query parameter (GET Query Mode only).
        The value is a JSON-serialised `BecknAction`. Mutually exclusive with the request body.
  responses:
    Ack:
      description: Receipt confirmed; signature valid; asynchronous callback will
        follow.
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/Ack'
    AckNoCallback:
      description: Request received but no callback will follow (e.g. agents not found,
        inventory unavailable).
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/AckNoCallback'
    NackBadRequest:
      description: Malformed or invalid request; the server could not parse or validate
        the payload.
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/NackBadRequest'
    NackUnauthorized:
      description: Invalid or missing authentication credentials; signature could
        not be verified.
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/NackUnauthorized'
    ServerError:
      description: Internal failure on the network participant's platform.
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/ServerError'
